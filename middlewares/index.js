const validateUser = require('./validateUser');
const validateExistence = require('./validateExistence');
const validateLogin = require('./validateLogin');

module.exports = { validateUser, validateExistence, validateLogin };
